import { DefaultTheme } from 'styled-components';

const DefTheme: DefaultTheme = {
  palette: {
    lightGray: '#ccc',
    darkGray: '#666',
    snowWhite: '#f1f2f2',
  },
};
export default DefTheme;
